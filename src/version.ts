/** The release shown in the sidebar and on the loading screen. scripts/bump_version.py rewrites
 *  it on every push, so a reader can tell whether the page in front of them has a change. */
export const APP_VERSION = 'v2.9.19';

# LRS Dashboard Environment Variables

# ==================================================
# EXISTING: Google Sheets Data Sync
# ==================================================
GOOGLE_SHEET_ID=your-google-sheet-id-here

# ==================================================
# NEW: AI-Powered Data Extraction Pipeline
# ==================================================

# Anthropic Claude API Key (required for AI extraction)
# Sign up at: https://console.anthropic.com/
# Cost: ~$1.50-4.00 to process all 75 simulants
ANTHROPIC_API_KEY=sk-ant-api03-xxx...your-key-here

# Google Sheets Service Account Credentials (required for writing)
# Path to the JSON file downloaded from Google Cloud Console
GOOGLE_SHEETS_CREDENTIALS=./google-service-account-credentials.json

# PDF Directory (optional, defaults to Spring directory)
PDF_DIRECTORY=~/Spring - Forest on the moon/DIRT

# Validation Settings (optional, defaults shown)
MIN_SOURCES_REQUIRED=2
CONFIDENCE_THRESHOLD=0.8

# ==================================================
# Usage:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual API keys and paths
# 3. Never commit .env to git (it's in .gitignore)
# ==================================================

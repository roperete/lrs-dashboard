[
 {
  "click": "Table"
 },
 {
  "hover": "tbody tr[data-row]:nth-child(1) sup button"
 },
 {
  "expect": "[...document.querySelectorAll('[role=tooltip]')].filter(t => parseFloat(getComputedStyle(t).opacity) > 0.5).length === 1",
  "why": "hovering one citation mark opens exactly one tooltip (2026-09-26: a row opened all of them)"
 },
 {
  "eval": "(async () => { const heads = [...document.querySelectorAll('thead th button')]; for (const h of heads) { h.click(); await new Promise(r => setTimeout(r, 120)); h.click(); await new Promise(r => setTimeout(r, 120)); } return heads.length; })()",
  "as": "sortsClicked"
 },
 {
  "eval": "(() => { const r = [...document.querySelectorAll('tbody tr[data-row]')].find(r => r.children[1]?.innerText.trim() === \"Lunar90\"); r?.click(); return !!r; })()",
  "as": "open_Lunar90"
 },
 {
  "wait": 900
 },
 {
  "eval": "(() => { const r = [...document.querySelectorAll('tbody tr[data-row]')].find(r => r.children[1]?.innerText.trim() === \"FEFU-1\"); r?.click(); return !!r; })()",
  "as": "open_FEFU-1"
 },
 {
  "wait": 900
 },
 {
  "eval": "(() => { const r = [...document.querySelectorAll('tbody tr[data-row]')].find(r => r.children[1]?.innerText.trim() === \"GreenSpar\"); r?.click(); return !!r; })()",
  "as": "open_GreenSpar"
 },
 {
  "wait": 900
 },
 {
  "eval": "(() => { const r = [...document.querySelectorAll('tbody tr[data-row]')].find(r => r.children[1]?.innerText.trim() === \"JSC-1A\"); r?.click(); return !!r; })()",
  "as": "open_JSC-1A"
 },
 {
  "wait": 900
 },
 {
  "focus": "tbody tr[data-row]"
 },
 {
  "key": "ArrowDown"
 },
 {
  "key": "ArrowDown"
 },
 {
  "key": "ArrowUp"
 },
 {
  "key": "Enter"
 },
 {
  "clickSel": "tbody tr[data-row] td:nth-child(16) button"
 },
 {
  "eval": "(() => { const sel = document.querySelector('select'); if (!sel) return 'no select'; const set = Object.getOwnPropertyDescriptor(HTMLSelectElement.prototype, 'value').set; const out = []; for (const o of sel.options) { set.call(sel, o.value); sel.dispatchEvent(new Event('change', { bubbles: true })); out.push(o.value || 'none'); } return out.join(','); })()",
  "as": "lunarRefs"
 },
 {
  "click": "Full comparison"
 },
 {
  "wait": 1200
 },
 {
  "key": "Escape"
 },
 {
  "key": "Escape"
 },
 {
  "clickSel": "tbody tr[data-row]",
  "after": 1500
 },
 {
  "expect": "[...document.querySelectorAll('a')].some(a => a.textContent.includes('Ask AI about this simulant') && a.href.includes('google.com/search'))",
  "why": "the pane's references end with Ask AI (owner, 2026-09-25)"
 },
 {
  "eval": "(() => { history.pushState({}, '', '?sim=S004'); dispatchEvent(new PopStateEvent('popstate')); return true; })()",
  "as": "open_BH1"
 },
 {
  "wait": 1500
 },
 {
  "expect": "![...document.querySelectorAll('th')].some(th => th.textContent.trim() === 'Apollo 16') && [...document.querySelectorAll('select option')].every(o => o.value !== 'Apollo 16')",
  "why": "a lunar sample with no shown values is neither suggested nor listed (BH-1 showed an Apollo 16 column of dashes)"
 },
 {
  "eval": "(() => { history.pushState({}, '', '?sim=S121'); dispatchEvent(new PopStateEvent('popstate')); return true; })()",
  "as": "open_BH2"
 },
 {
  "wait": 1500
 },
 {
  "click": "Full comparison",
  "after": 2000
 },
 {
  "expect": "(() => { const t = [...document.querySelectorAll('table')].find(t => t.textContent.includes('Component')); if (!t) return false; const cells = [...t.querySelectorAll('tbody tr')].map(r => r.children[1]).filter(c => c && c.textContent.trim() !== '\u2014'); return cells.length > 0 && cells.every(c => c.querySelector('sup')); })()",
  "why": "every simulant value in the lunar comparison carries its citation mark"
 }
]